/**
 * Imports necessary libraries and components to mount the Vue application.
 */
import { createApp } from 'vue';
import App from './App.vue';

/**
 * Creates a new Vue application instance and mounts it to the element with ID "app".
 */
createApp(App).mount('#app');
